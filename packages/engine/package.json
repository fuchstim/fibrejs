{
  "name": "@fibrejs/engine",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "build": "tsc -p ./tsconfig.json",
    "rebuild": "npm run clean && npm run build",
    "watch": "tsc -b -w",
    "test": "mocha ./test --config ../../.mocharc.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fibrejs/logger": "^1.0.0",
    "@types/uuid": "^9.0.0",
    "uuid": "^9.0.0"
  }
}
